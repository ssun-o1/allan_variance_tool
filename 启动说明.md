# 艾伦方差分析工具 - 启动说明

## 关于启动时间

当你双击 `AllanVarianceTool.app` 时：

1. **首次启动**: 可能需要 2-5 秒加载时间
   - PyInstaller 打包的应用需要解压临时文件
   - 会看到一个加载窗口显示"正在加载，请稍候..."
   - 加载窗口有动态点点动画效果

2. **加载窗口功能**:
   - 显示应用标题（中英文）
   - 显示加载进度动画（三个闪烁的圆点）
   - 加载完成后自动切换到主窗口

3. **后续启动**: 如果 macOS 缓存了临时文件，启动会更快

## 如果看不到加载窗口

如果启动时没有任何反应：
- 请耐心等待 3-5 秒
- macOS 可能在后台验证应用签名
- 如果首次运行，可能需要在"系统偏好设置 > 安全性与隐私"中允许应用运行

## 技术说明

- 加载窗口采用深色主题设计
- 背景色: #0F131C (深蓝灰色)
- 强调色: #38BDF8 (亮蓝色)
- 无边框设计，居中显示
- 窗口大小: 400x200 像素

## 改进内容

✅ 添加了启动加载窗口
✅ 提供视觉反馈，避免用户误以为应用卡死
✅ 动态加载动画
✅ 优雅的颜色方案
✅ 自动关闭并切换到主窗口
